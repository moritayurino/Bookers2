    <p id="notice"></p>
        <div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
      <%= render 'users/book', users: @users %>
      <h2>Users</h2>
      <table class='table'>
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th></th>
    </tr>
    <% @users.each do |user| %>
    <tr>
      <th><%= image_tag user.get_profile_image(100, 100) %></th>
      <th><%= user.name %></th>
      <th><%= link_to "Show", user_path(user.id) %></th>
      <% end %>
      <th colspan="3">
         </tr>
         </thead>
  <tbody>
    </tbody>
</table>
    </div>
  </div>
</div>