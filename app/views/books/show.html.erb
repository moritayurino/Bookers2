<% if flash[:notice] %>
  <div class="alert alert-success">
    <%= flash[:notice] %>
  </div>
  <% end %>
  <% if flash[:success] %>
  <div class="alert alert-success">
    <%= flash[:success] %>
  </div>
<% end %>
      <p id="notice"></p>
        <div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
      <%= render partial: 'users/book', locals: {user: @user, book: @book} %>
      <h2>Book detail</h2>
      <table class='table table-hover table-inverse'>
      <thead>
      <tr>
      <th><%= link_to book_path(@books.id) do %><%= image_tag @user.get_profile_image(100, 100) %><% end %></th>
      <th></th>
      <th><%= link_to @books.title, book_path(@books.id) %></th>
      <th><%= @books.body %></th>
      <% if @books.user == current_user %>
      <th><%= link_to "Edit", edit_book_path(@books.id), method: :get, class:"btn btn-outline-secondary edit_book_path" %></th>
      <th><%= link_to "Destroy", book_path, method: :delete, class:"btn btn-outline-secondary books_path" %></th>
      <% end %></tr>
      </thead>
  <tbody>
  </tbody>
</table><%= link_to @books.user.name, user_path(@books.user.id) %>

      <th colspan="3">
    </div>
  </div>
</div>